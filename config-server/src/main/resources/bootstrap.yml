spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/rawatvinod4164/spring-config.git
          search-paths: '{application}'
#          refreshRate: 60
server:
  port: 8888